{
  "name": "lambda-static-server",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run vite",
    "build-prod": "tsc && npm run download-yt-dlp && npm run vite",
    "vite": "cd public && vite build",
    "prepack": "npm run build",
    "download-yt-dlp": "node  scripts/download-yt-dlp.js",
    "ts-node": "ts-node"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.858.0",
    "@aws-sdk/client-dynamodb": "^3.858.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "vite": "^7.0.6",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
